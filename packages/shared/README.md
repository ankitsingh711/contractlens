# packages/shared

Reserved for types/utilities shared between `apps/web` and `apps/api` tooling (e.g., generated OpenAPI types, shared constants). Empty until a concrete cross-app duplication shows up worth extracting.
